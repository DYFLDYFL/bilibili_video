# 本地配置（Cookie 路径、控制台、yt-dlp 浏览器 Cookie 等）
config.ps1

# 旧版本遗留的本地链接
saved/

# 运行时（bilibili-cookies.txt、diag.log、play-state.json、play.pid、播放缓冲等）
runtime/
runtime/danmaku-*.jsonl

# Poe MCP（个人模型调用，与 B 站播放项目无关）
.poe/

# Cursor 规则（本地 IDE 配置，不进仓库）
.cursor/rules/

# 本地工具
tools/webview2/
tools/live-danmaku/node_modules/

dist/

Desktop.ini
